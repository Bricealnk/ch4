{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Ajout projet {% endblock %}
{% block body %}
    <div class="container">
        <div class="row justify-content-md-center">
            <div class="col-lg-6 col-sm-10 p-4 ">
                <h1 class="col-10 col-sm-6 bg-green text-light text-center p-2 m-3 rounded-pill fw-bold fs-3"></h1>

    <h1 class="text-center fw-bold my-3 py-3 text-dark">Ajouter un projet</h1>
    <div class="justify-content-center col-5  fw-bold  bg-primary">
        {{ form_start(form) }}
        {{ form_widget(form) }}
    </div>
    <div class="justify-content-center">
        <button type="submit" class="btn rounded-pill btn-lg fw-bold btn-success bg-primary">Valider</button>
    </div>
    </div>
    </div>
    </div>
    {{ form_end(form) }}
{% endblock %}